import styled from 'styled-components';

export const HeaderContainer = styled.header`
  background: linear-gradient(0deg, rgba(20,88,156,0.2) 10%, rgba(11,27,43,0.1) 90%);

  display: flex;
  justify-content: space-between;

  padding: 2rem 0;

  div {
    display: flex;
    align-items: center;
    margin-bottom: 2.875rem;
  }
`